import { DateCountPipe } from './date-count.pipe.ts.component';

describe('DateCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCountPipe();
    expect(pipe).toBeTruthy();
  });
});